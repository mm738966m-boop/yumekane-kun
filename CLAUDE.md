# プロジェクト概要
このプロジェクトは「ユメカネくん」というシステムの開発リポジトリです。
Claudeは、以下のルールとコマンドを理解し、今後のコード生成やレビューのベースとしてください。

## 1. よく使うコマンド
Claudeが自律的にタスクを実行したり、テストを行う際は以下のコマンドを使用してください。
- 開発サーバーの起動: `npm run dev`
- パッケージのインストール: `npm install`
- テストの実行: `npm run test`
- コード整形 (Lint): `npm run lint`

## 2. コーディング規約 (ルール)
コードを修正・生成する際は、以下のルールを厳守してください。
- 言語・技術スタック: 主に TypeScript を使用します。
- 命名規則:
  - 変数名・関数名: キャメルケース (`exampleVariableName`)
  - クラス名: パスカルケース (`ExampleClass`)
- コメント: 複雑な処理や、直感的にわかりにくい計算ロジックには、必ず日本語でコメントを残してください。
- エラー処理: 予期せぬエラーが起きそうな場所には `try-catch` を実装し、ログを出力してください。

## 3. ディレクトリ構造のルール
新しくファイルを作成する際は、用途に合わせて以下の場所に配置してください。
- 画面の見た目に関するファイル: `src/components/`
- 全体で使い回す便利な関数: `src/utils/`
- APIや外部通信に関する処理: `src/api/`
